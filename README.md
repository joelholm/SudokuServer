This is the backend for the SudokuWeb application.

Run 'python manage.py runserver' to start the server on localhost:8000.

This will provide APIs for the frontend in Angular to start playing Sudoku
